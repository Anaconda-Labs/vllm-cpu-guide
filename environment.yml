name: vllm-gpu-demo
channels:
  - conda-forge
  - nvidia
  - defaults
dependencies:
  - python=3.11
  - pip
  - cuda-toolkit=12.1
  - pip:
      - vllm==0.6.0
      - requests==2.31.0
      - torch==2.4.0
